{
  "name": "@favoritable/config-biome",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared Biome configuration for the favoritable monorepo",
  "files": [
    "biome.json"
  ],
  "exports": {
    ".": "./biome.json"
  },
  "scripts": {
    "knip": "knip",
    "lint": "biome lint --error-on-warnings",
    "lint:fix": "pnpm lint --write",
    "format": "biome format",
    "format:fix": "pnpm format --write",
    "check": "biome check",
    "check:fix": "pnpm check --write"
  },
  "dependencies": {
    "@biomejs/biome": "~2.1.4"
  },
  "devDependencies": {
    "knip": "~5.62.0"
  }
}
