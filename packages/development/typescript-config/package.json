{
  "name": "@favoritable/typescript-config",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "knip": "knip",
    "lint": "pnpm format:check",
    "lint:fix": "pnpm format",
    "format": "pnpm -w exec oxfmt --config .oxfmtrc.json packages/development/typescript-config",
    "format:check": "pnpm -w exec oxfmt --config .oxfmtrc.json --check packages/development/typescript-config",
    "format:fix": "pnpm format",
    "check": "pnpm format:check",
    "check:fix": "pnpm format"
  },
  "devDependencies": {
    "knip": "~5.62.0",
    "typescript": "~5.7.3"
  }
}
